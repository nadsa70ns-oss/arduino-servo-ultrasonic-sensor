# arduino-servo-ultrasonic-sensor
Arduino project using an ultrasonic sensor (HC-SR04) to control a servo motor — the servo moves to a set angle when an object is detected within range and returns to rest when the object moves away.
